name: leadpulse
version: "1.0.0"
config-version: 2

profile: leadpulse

model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - target
  - dbt_packages
  - logs

models:
  leadpulse:
    staging:
      +materialized: view
      +schema: staging

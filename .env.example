# Safe local-development example. Copy to .env and change the password locally.
# Never commit .env or a real credential.
POSTGRES_DB=leadpulse
POSTGRES_USER=leadpulse
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Local-development contract shared by Docker Compose, dbt, ingestion, and dashboard.
# Copy this file to .env. Never commit .env or use these values in production.
POSTGRES_DB=leadpulse
POSTGRES_USER=leadpulse
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=55433
